import { describe, it } from "@effect/vitest";

import * as Effect from "effect/Effect";

describe.skip("wireguard generate vpn tunneled access", () => {
    it.effect("Should generate configs", () => Effect.gen(function* () {}));
});
